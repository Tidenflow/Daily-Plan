# =====算法 leetcode=====
260320已经按时完成
260321预计学习完成   滑动窗口 -> 不定长  -> 越长越合法（求最短/最小）part1
- [ ]209. 长度最小的子数组
- [ ]3795. 不同元素和至少为 K 的最短子数组长度 1505
- [ ]2904. 最短且字典序最小的美丽子字符串 做到 O(n2)



# =====TS=====

## 明日计划（2026-03-21）

### 今日回顾
已学完：基础类型（原始类型/数组/元组/枚举/any/unknown/void/never/字面量与断言）+ 函数与接口（函数类型、重载、this、接口、索引签名、继承、合并）

### 明日练习内容（预计 2-3 小时，偏实战）

#### 1. 类基础检验
- [ ] 新建 `03-Class-generic/class.ts`
- [ ] 定义 `Person` 类：属性 `name`、`age`（readonly）、`id`（private）
- [ ] 定义构造函数 `constructor(name: string, age: number)`
- [ ] 定义方法 `introduce(this: Person): string`
- [ ] 要求：使用 `public`、`private`、`readonly` 修饰符各至少 1 次

#### 2. 类继承与 super 检验
- [ ] 新建 `03-Class-generic/class-inheritance.ts`
- [ ] 定义父类 `Animal`：属性 `name`，方法 `speak(): string`
- [ ] 定义子类 `Dog` 继承 `Animal`，添加属性 `breed: string`
- [ ] 重写 `speak` 方法，使用 `super.speak()` 调用父类
- [ ] 要求：演示 super 在构造函数、方法中的使用

#### 3. 抽象类检验
- [ ] 新建 `03-Class-generic/abstract-class.ts`
- [ ] 定义抽象类 `Shape`：抽象方法 `getArea(): number`，属性 `color`
- [ ] 定义 `Circle` 实现 `Shape`：`radius` 属性，实现 `getArea()`
- [ ] 定义 `Rectangle` 实现 `Shape`：`width`、`height` 属性，实现 `getArea()`
- [ ] 要求：包含抽象属性、抽象方法、具体实现

#### 4. 泛型函数检验
- [ ] 新建 `03-Class-generic/generic-function.ts`
- [ ] 实现 `identity<T>(value: T): T`（返回原值）
- [ ] 实现 `pickFirst<T, K extends keyof T>(obj: T, key: K): T[K]`
- [ ] 实现 `createPair<S, T>(first: S, second: T): [S, T]`
- [ ] 要求：至少 1 个使用约束 `extends`

#### 5. 泛型接口检验
- [ ] 新建 `03-Class-generic/generic-interface.ts`
- [ ] 定义 `Container<T>` 接口：`value: T`、`getValue(): T`
- [ ] 定义 `Pair<S, T>` 接口：方法 `setFirst(v: S): void`、`setSecond(v: T): void`、`getBoth(): [S, T]`
- [ ] 实现 `StringContainer` 实现 `Container<string>`
- [ ] 要求：演示泛型接口的两种实现方式（指定类型/保留泛型）

#### 6. 自测标准（必须完成）
- [ ] 运行 `tsc --noEmit` 无报错
- [ ] 每个文件至少写 2 组调用示例
- [ ] 写 5 行以内总结：说明 类 vs 接口、抽象类 vs 接口 的使用场景

### 学习资源
- TypeScript 官方文档：Classes / Generics
- 你本周笔记：`docs/260319-TS-Notes.md`、`docs/260320-TS-Practice-Notes.md`

### 目标
明天结束后能够：在不看笔记的情况下，独立写出类的继承、super 使用、抽象类定义、泛型函数与接口，并通过 `tsc` 类型检查


# =====Cpp=====

周六踢理学院，cpp停一天 ⚽