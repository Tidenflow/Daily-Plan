# =====算法 leetcode=====
##预计学习完成   滑动窗口 -> 不定长  -> 越短越合法（求最长/最大）part2
- [ ]1208. 尽可能使字符串相等 1497
- [ ]904. 水果成篮 1516
- [ ]1695. 删除子数组的最大得分 1529
- [ ]2958. 最多 K 个重复元素的最长子数组 1535
- [ ]2024. 考试的最大困扰度 1643
- [ ]1004. 最大连续 1 的个数 III 1656



# =====TS=====

## 明日计划（2026-03-20）

### 今日回顾
已学完：基础类型（原始类型/数组/元组/枚举/any/unknown/void/never/字面量与断言）+ 函数与接口（函数类型、重载、this、接口、索引签名、继承、合并）

### 明日练习内容（预计 2-3 小时，偏实战）

#### 1. 基础类型与字面量检验
- [ ] 新建 `practice/basic.ts`
- [ ] 定义 `UserRole = "admin" | "user"`、`ApiStatus = "SUCCESS" | "ERROR"`
- [ ] 实现 `formatUser(role, status)` 返回模板字符串
- [ ] 要求：使用字面量类型、类型别名、`as const` 各至少 1 次

#### 2. 函数参数与重载检验
- [ ] 新建 `practice/function.ts`
- [ ] 实现 `parseInput` 重载：
- [ ] 传入 `string` -> 返回 `string[]`（按逗号切分）
- [ ] 传入 `number` -> 返回 `number`（平方）
- [ ] 传入 `boolean` -> 返回 `"YES" | "NO"`
- [ ] 要求：包含可选参数 1 个、默认参数 1 个

#### 3. this 与接口检验
- [ ] 新建 `practice/this-and-interface.ts`
- [ ] 定义 `Student` 接口：`readonly id`、`hobby?`、`updateAge(this: Student, age: number): void`
- [ ] 分别演示 `call`、`apply`、`bind` 各 1 次绑定 `this` 调用

#### 4. 索引签名综合检验
- [ ] 新建 `practice/dictionary.ts`
- [ ] 定义混合索引接口：数字索引返回 `string`，字符串索引返回 `string | number`
- [ ] 实现 `pickStringValues`：提取对象中所有字符串值，返回 `string[]`

#### 5. 自测标准（必须完成）
- [ ] 运行 `tsc --noEmit` 无报错
- [ ] 每个文件至少写 2 组调用示例
- [ ] 写 5 行以内总结：说明 `any` vs `unknown`、`interface` vs `type` 的使用取舍

### 学习资源
- TypeScript 官方文档：Functions / Interfaces / Everyday Types
- 你本周笔记：`docs/2603318-TS-Notes.md`、`docs/260319-TS-Notes.md`

### 目标
明天结束后能够：在不看笔记的情况下，独立写出函数重载、this 绑定调用、接口与索引签名的常见写法，并通过 `tsc` 类型检查


# =====Cpp=====

今日大脑🧠宕机
早八下午上课太累了
将  260319任务转移至260320

### 明日学习内容（预计 1-2 小时）

#### 1. 拷贝构造函数（重点）
- [ ] 什么是拷贝构造
- [ ] 默认拷贝构造（浅拷贝）
- [ ] 深拷贝 vs 浅拷贝
- [ ] 拷贝构造的调用时机

#### 2. 指针与引用
- [ ] 指针基本语法（* &）
- [ ] 指针 vs 引用
- [ ] 传值 vs 传指针 vs 传引用
- [ ] const 指针与引用

#### 3. 内存管理（堆 vs 栈）
- [ ] 栈上分配 vs 堆上分配
- [ ] new / delete 操作
- [ ] 指针与堆内存
- [ ] 内存泄漏概念

#### 4. 调试器入门（VSCode/GDB）
- [ ] 设置断点
- [ ] 查看变量值
- [ ] 查看调用栈（Stack Trace）
- [ ] 监视内存地址

### 学习资源
- C++ 经典教程：Bilibili 侯捷 C++ 系列
- 练习项目：实现一个简单的 String 类（包含拷贝构造）

### 目标
明天结束后能够：理解拷贝构造原理，会使用指针和引用，能区分堆栈内存，会用调试器查看函数调用栈
